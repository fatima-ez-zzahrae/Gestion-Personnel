<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="2dp"
    app:cardCornerRadius="8dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Photo -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/ivPhoto"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_person_placeholder"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize50Percent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Nom complet -->
        <TextView
            android:id="@+id/tvNomComplet"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Nom Prénom"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toTopOf="@id/ivPhoto"
            app:layout_constraintStart_toEndOf="@id/ivPhoto"
            app:layout_constraintEnd_toStartOf="@id/btnMore"/>

        <!-- PPR -->
        <TextView
            android:id="@+id/tvPpr"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="PPR: 12345678"
            android:textSize="14sp"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/tvNomComplet"
            app:layout_constraintStart_toStartOf="@id/tvNomComplet"
            app:layout_constraintEnd_toEndOf="@id/tvNomComplet"/>

        <!-- Grade et Type -->
        <TextView
            android:id="@+id/tvGradeType"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Grade • Type"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="2dp"
            app:layout_constraintTop_toBottomOf="@id/tvPpr"
            app:layout_constraintStart_toStartOf="@id/tvNomComplet"
            app:layout_constraintEnd_toStartOf="@id/chipStatus"/>

        <!-- Badge Actif/Inactif -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Actif"
            android:textSize="10sp"
            style="@style/Widget.Material3.Chip.Assist"
            app:layout_constraintTop_toTopOf="@id/tvGradeType"
            app:layout_constraintBottom_toBottomOf="@id/tvGradeType"
            app:layout_constraintEnd_toStartOf="@id/btnMore"/>

        <!-- Menu 3 points -->
        <ImageButton
            android:id="@+id/btnMore"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_more_vert"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>

